@@ -67,8 +69,8 @@
     }
   }, [])
 
-  const claimedRestaurants = useMemo(() => {
-    return restaurants.filter((restaurant) => restaurant.ownerId === user?.userId)
+  const managedRestaurants = useMemo(() => {
+    return restaurants.filter((restaurant) => restaurant.ownerId === user?.userId || restaurant.createdBy === user?.userId)
   }, [restaurants, user?.userId])
 
   async function handleSave(event) {
@@ -275,10 +283,10 @@
             </section>
 
             <section className='form-card'>
-              <h2>My Claimed Restaurants</h2>
-              {claimedRestaurants.length > 0 ? (
+              <h2>My Managed Restaurants</h2>
+              {managedRestaurants.length > 0 ? (
                 <div className='list-stack'>
-                  {claimedRestaurants.map((restaurant) => (
+                  {managedRestaurants.map((restaurant) => (
                     <div key={restaurant.id} className='list-item'>
                       <strong>{restaurant.name}</strong>
                       <span className='muted'>
@@ -296,7 +304,7 @@
                   ))}
                 </div>
               ) : (
-                <p className='muted'>No claimed restaurants yet.</p>
+                <p className='muted'>No managed restaurants yet. Owners who add a new restaurant will also see it here.</p>
               )}
             </section>
           </aside>
